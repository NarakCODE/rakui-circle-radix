# Task Checklist

- [ ] Inspect existing hooks, i18n helpers, and UI primitives for reusable language-switching patterns
- [ ] Create shared language types and config for supported locales and labels
- [ ] Add or update locale-aware navigation helpers in `i18n/navigation.ts`
- [ ] Implement a shared `use-language-switcher` hook that preserves pathname and query params
- [ ] Build accessible `LanguageSwitcher`, `LanguageDropdown`, and `LanguageSelect` components without duplicating logic
- [ ] Verify the new components with type checking and targeted linting

# Results

- Pending
